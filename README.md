create a samba domain and squid proxy with rocky9-minial docker image
======================
* create an image with samba and deploy the domain settings
* create an image with squid proxy and join the domain created above
